services:
  - type: web
    name: intel-hub
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run src/ui_us.py --server.port $PORT --server.address 0.0.0.0
    autoDeploy: true
    healthCheckPath: /
