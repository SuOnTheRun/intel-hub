[server]
headless = true
enableCORS = false
enableXsrfProtection = true
address = "0.0.0.0"
port = 8501
# Helps with flaky free-tier proxies on first WebSocket connect
websocketPingTimeout = 30
maxMessageSize = 200

[client]
toolbarMode = "viewer"
